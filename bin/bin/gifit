#!/bin/sh
# turn a video into a gif
#
# deps:
#  - ffmpeg
#  - gifsicle
#
# charlie (c) 2015

if [ -z "$1" ]; then
    echo "usage: `basename $0` moviefile"
    exit
fi

FILE=$1
ffmpeg -i $FILE -r 60 /tmp/$FILE.gif
gifsicle --delay=3 --optimize=3 /tmp/$FILE.gif > $FILE.gif

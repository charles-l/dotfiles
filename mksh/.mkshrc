HISTFILE=$HOME/.history
. $HOME/.profile

eval $(perl -I${HOME}/perl5/lib/perl5 -Mlocal::lib)

export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting

# gimme a ron quote
printf "\033[1;37m\n  `cat /tmp/quote`\n    -- Ron Swanson\n\n\033[0m"
(curl -s http://ron-swanson-quotes.herokuapp.com/v2/quotes | tr -d '"[]' > /tmp/quote &)

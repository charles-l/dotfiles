# vim: set ft=sxhkdrc:

super + {h,j,k,l}
  closest {h,j,k,l}

XF86AudioRaiseVolume
  volume + ; echo "volume: $(volume level)" | popup

XF86AudioLowerVolume
  volume - ; echo "volume: $(volume level)" | popup

XF86AudioMute
  volume ! ; echo "volume $(volume state)" | popup
